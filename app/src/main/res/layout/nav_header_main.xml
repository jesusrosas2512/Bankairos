<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_max_height"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/side_nav_bar"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingLeft="@dimen/common_default"
    android:paddingTop="@dimen/common_default"
    android:paddingRight="@dimen/common_default"
    android:paddingBottom="@dimen/common_default"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo"
        android:layout_gravity="end"
        android:layout_marginBottom="@dimen/common_big"
        tools:ignore="ContentDescription" />
    
</LinearLayout>